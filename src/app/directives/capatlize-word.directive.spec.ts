import { CapatlizeWordDirective } from './capatlize-word.directive';

describe('CapatlizeWordDirective', () => {
  it('should create an instance', () => {
    const directive = new CapatlizeWordDirective();
    expect(directive).toBeTruthy();
  });
});
